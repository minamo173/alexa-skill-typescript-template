{
  "name": "skill",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc && cp ./package.json ./../dist/package.json && cd ./../dist/ && yarn install --production"
  },
  "devDependencies": {
    "@types/alexa-sdk": "^1.1.1",
    "@types/aws-lambda": "^8.10.6",
    "@types/node": "^10.3.2",
    "typescript": "^2.9.1"
  },
  "dependencies": {
    "ask-sdk": "^2.0.5"
  }
}
